const {
  Get,
  Controller,
  ApplicationComponent,
} = require('@clusic/method');

@Controller('/')
class <%=className%> extends ApplicationComponent {
  constructor(ctx) {
    super(ctx);
  }
  
  @Get('/')
  async welcome() {
    this.ctx.body = 'Welcome to use Rex';
  }
}

module.exports = <%=className%>;