module.exports = async app => {
  // app.bind('routing', () => {});
  // app.bind('beforeStart', () => {});
  // app.bind('starting', () => {});
  // app.bind('started', () => {});
  // app.bind('beforeStop', () => {});
  // app.bind('stop', () => {});
  // app.bind('ready', () => {});
  // app.bind('decorate', ({ property, prefix, getter, extras, controller, result }) => {});
};